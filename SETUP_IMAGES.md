# Інструкція: Додавання PNG зображень медичного обладнання

## 📋 Необхідні зображення

Збережіть наступні зображення у папку `public/images/`:

### 1. **mini-fridge.png**
- Зображення: Міні-холодильник (сірий)
- Розмір: оптимально 800x800px або менше
- Формат: PNG з прозорим фоном (якщо можливо)

### 2. **air-purifier.png**
- Зображення: Очищувач повітря (білий з дисплеєм)
- Розмір: оптимально 800x1200px або менше
- Формат: PNG з прозорим фоном

### 3. **hospital-bed-1.png**
- Зображення: Лікарняне ліжко з синіми деталями (вид 1)
- Розмір: оптимально 1200x800px або менше
- Формат: PNG з прозорим фоном

### 4. **hospital-bed-2.png**
- Зображення: Лікарняне ліжко з синіми деталями (вид 2)
- Розмір: оптимально 1200x800px або менше
- Формат: PNG з прозорим фоном

### 5. **oxygen-cylinder.png**
- Зображення: Кисневий балон
- Розмір: оптимально 600x1200px або менше
- Формат: PNG з прозорим фоном

## 🚀 Швидке налаштування

### Автоматичний спосіб (PowerShell):

```powershell
# Перейдіть у папку проекту
cd c:\infimary-3d-overlay

# Створіть папку images (якщо ще не створена)
New-Item -ItemType Directory -Force -Path "public\images"

# Скопіюйте ваші PNG файли у public\images\
# Наприклад:
Copy-Item "C:\Downloads\mini-fridge.png" -Destination "public\images\"
Copy-Item "C:\Downloads\air-purifier.png" -Destination "public\images\"
Copy-Item "C:\Downloads\hospital-bed-1.png" -Destination "public\images\"
Copy-Item "C:\Downloads\hospital-bed-2.png" -Destination "public\images\"
Copy-Item "C:\Downloads\oxygen-cylinder.png" -Destination "public\images\"
```

### Ручний спосіб:

1. Відкрийте папку `c:\infimary-3d-overlay\public\images\`
2. Збережіть там 5 PNG файлів з назвами, що вказані вище
3. Перезапустіть сервер розробки: `npm start`

## ✅ Перевірка

Після додавання зображень:

1. Запустіть проект: `npm start`
2. Відкрийте редактор
3. Натисніть "☰ Menu"
4. Прокрутіть до розділу **"Medical Equipment (PNG)"**
5. Ви побачите 5 кнопок з назвами обладнання
6. Натисніть будь-яку кнопку - об'єкт з'явиться на полотні

## 🔧 Налаштування розмірів

Якщо зображення виглядають занадто великими/малими, можна налаштувати їх у файлі:
`src/components/CameraOverlay.tsx` в функції `handleAddObject`

```typescript
// Знайдіть цей код і змініть scaleFactor:
const isMobile = window.innerWidth <= 768;
const scaleFactor = isMobile ? 0.3 : 0.4; // Збільште/зменште значення
```

## 📝 Додавання більше зображень

Щоб додати більше медичного обладнання:

1. Додайте PNG файл у `public/images/`
2. Відкрийте `src/components/ObjectToolbar.tsx`
3. Додайте новий елемент у масив `DEFAULT_IMAGES`:

```typescript
const DEFAULT_IMAGES: Array<{ name: string; path: string; type: 'image' }> = [
  // ... існуючі об'єкти
  { name: "Ваша назва", path: "/images/ваш-файл.png", type: 'image' },
];
```

4. Збережіть файл - зміни застосуються автоматично!

---

**Важливо**: Усі файли повинні бути в форматі PNG для найкращої якості і підтримки прозорості.
